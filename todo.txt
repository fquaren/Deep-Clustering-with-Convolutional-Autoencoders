TODO

* add 2d layer for cluster plotting
* restructure repo:
    * data in one directory 
    * make notebook into a python script
    * notebooks in one directory
    * change all paths in code!
    * experiments folder 
* experiment on hyperparameters to find the best results
